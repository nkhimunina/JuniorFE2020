// Вывести уравнение прямой по координатам двух точек

// Каноническое уравнение прямой, проходящей через две точки:
// (x - x1)/(x2 - x1) = (y - y1)/(y2 - y1)

let x1 = -5;
let y1 = 0.6;
let x2 = 1;
let y2 = -0.3;

console.log("(x - ", x1, ")/", x2 - x1, " = (y - ", y1, ")/", y2 - y1);